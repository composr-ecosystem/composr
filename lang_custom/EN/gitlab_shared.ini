[strings]
GITLAB=GitLab

GITLAB_ACCESS_TOKEN=GitLab access token
CONFIG_OPTION_gitlab_access_token=The private token which will be used for GitLab REST API calls (it should have the api scope and should be a project / group token, not a personal token).
